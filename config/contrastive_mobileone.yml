# contrastive loss 训练配置（MobileOne 调优建议）
# 如未在 CLI 指定，将被 train_contrastive 在 loss_type=contrastive 时读取。
learning_rate: 0.0005
epochs: 40
batch_size: 16
feature_dim: 128
margin: 0.4
weight_decay: 0.0001
lr_step_size: 12
lr_gamma: 0.5
